This wonderful app makes it easy as a pie to automatically set up virtual
machines with application demos to demonstrate them to your customers.

See vm-spawn/examples/vm-spawn.pl to see what the usage looks like.
